#include "main.h"

/**
 * _abs - entry function
 * computes the absolute value of an integer
 * @n: a number to get it's absolute value
 * Return: the absolute value of given number
 */
int _abs(int n)
{
return (n < 0 ? n * -1 : n);
}
